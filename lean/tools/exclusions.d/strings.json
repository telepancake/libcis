{
  "_comment": "strings/ subtree lean exclusions. Base gate is CLEAN for all 368 strings/ run tests, so every entry here is a LEAN-ONLY failure traced to a documented lean/README.md deviation. Categories: (A) custom-allocator instantiations [deviation 1]; (B) constexpr tests that ALSO use a custom allocator [deviations 1+2+3]; (C) pure constexpr tests whose runtime path passes but whose static_assert(test()) constexpr-constructs a NON-EMPTY string [deviations 2+3]. lean makes the EMPTY std::string constexpr-constructible (matching base's SSO-empty slice); non-empty constexpr construction is the documented no-constexpr-heap / no-SSO deviation.",
  "strings/basic.string/string.capacity/capacity.pass.cpp": "lean deviation 1 (custom allocators): this test instantiates std::basic_string with a non-std allocator (test_allocator/min_allocator/safe_allocator/limited_alloc) or checks allocator-dependent noexcept/propagation. lean supports only std::allocator<CharT> (enforced by static_assert), so the instantiation is rejected at compile time. See lean/README.md deviation 1.",
  "strings/basic.string/string.capacity/reserve.pass.cpp": "lean deviation 1 (custom allocators): this test instantiates std::basic_string with a non-std allocator (test_allocator/min_allocator/safe_allocator/limited_alloc) or checks allocator-dependent noexcept/propagation. lean supports only std::allocator<CharT> (enforced by static_assert), so the instantiation is rejected at compile time. See lean/README.md deviation 1.",
  "strings/basic.string/string.cons/alloc.pass.cpp": "lean deviation 1 (custom allocators): this test instantiates std::basic_string with a non-std allocator (test_allocator/min_allocator/safe_allocator/limited_alloc) or checks allocator-dependent noexcept/propagation. lean supports only std::allocator<CharT> (enforced by static_assert), so the instantiation is rejected at compile time. See lean/README.md deviation 1.",
  "strings/basic.string/string.cons/dtor.pass.cpp": "lean deviation 1 (custom allocators): this test instantiates std::basic_string with a non-std allocator (test_allocator/min_allocator/safe_allocator/limited_alloc) or checks allocator-dependent noexcept/propagation. lean supports only std::allocator<CharT> (enforced by static_assert), so the instantiation is rejected at compile time. See lean/README.md deviation 1.",
  "strings/basic.string/string.cons/implicit_deduction_guides.pass.cpp": "lean deviation 1 (custom allocators): this test instantiates std::basic_string with a non-std allocator (test_allocator/min_allocator/safe_allocator/limited_alloc) or checks allocator-dependent noexcept/propagation. lean supports only std::allocator<CharT> (enforced by static_assert), so the instantiation is rejected at compile time. See lean/README.md deviation 1.",
  "strings/basic.string/string.cons/iter_alloc.pass.cpp": "lean deviation 1 (custom allocators): this test instantiates std::basic_string with a non-std allocator (test_allocator/min_allocator/safe_allocator/limited_alloc) or checks allocator-dependent noexcept/propagation. lean supports only std::allocator<CharT> (enforced by static_assert), so the instantiation is rejected at compile time. See lean/README.md deviation 1.",
  "strings/basic.string/string.cons/iter_alloc_deduction.pass.cpp": "lean deviation 1 (custom allocators): this test instantiates std::basic_string with a non-std allocator (test_allocator/min_allocator/safe_allocator/limited_alloc) or checks allocator-dependent noexcept/propagation. lean supports only std::allocator<CharT> (enforced by static_assert), so the instantiation is rejected at compile time. See lean/README.md deviation 1.",
  "strings/basic.string/string.cons/move_alloc.pass.cpp": "lean deviation 1 (custom allocators): this test instantiates std::basic_string with a non-std allocator (test_allocator/min_allocator/safe_allocator/limited_alloc) or checks allocator-dependent noexcept/propagation. lean supports only std::allocator<CharT> (enforced by static_assert), so the instantiation is rejected at compile time. See lean/README.md deviation 1.",
  "strings/basic.string/string.cons/move_assign_noexcept.pass.cpp": "lean deviation 1 (custom allocators): this test instantiates std::basic_string with a non-std allocator (test_allocator/min_allocator/safe_allocator/limited_alloc) or checks allocator-dependent noexcept/propagation. lean supports only std::allocator<CharT> (enforced by static_assert), so the instantiation is rejected at compile time. See lean/README.md deviation 1.",
  "strings/basic.string/string.cons/move_noexcept.pass.cpp": "lean deviation 1 (custom allocators): this test instantiates std::basic_string with a non-std allocator (test_allocator/min_allocator/safe_allocator/limited_alloc) or checks allocator-dependent noexcept/propagation. lean supports only std::allocator<CharT> (enforced by static_assert), so the instantiation is rejected at compile time. See lean/README.md deviation 1.",
  "strings/basic.string/string.cons/string_view_deduction.pass.cpp": "lean deviation 1 (custom allocators): this test instantiates std::basic_string with a non-std allocator (test_allocator/min_allocator/safe_allocator/limited_alloc) or checks allocator-dependent noexcept/propagation. lean supports only std::allocator<CharT> (enforced by static_assert), so the instantiation is rejected at compile time. See lean/README.md deviation 1.",
  "strings/basic.string/string.cons/string_view_size_size_deduction.pass.cpp": "lean deviation 1 (custom allocators): this test instantiates std::basic_string with a non-std allocator (test_allocator/min_allocator/safe_allocator/limited_alloc) or checks allocator-dependent noexcept/propagation. lean supports only std::allocator<CharT> (enforced by static_assert), so the instantiation is rejected at compile time. See lean/README.md deviation 1.",
  "strings/basic.string/string.modifiers/string_assign/string.pass.cpp": "lean deviation 1 (custom allocators): this test instantiates std::basic_string with a non-std allocator (test_allocator/min_allocator/safe_allocator/limited_alloc) or checks allocator-dependent noexcept/propagation. lean supports only std::allocator<CharT> (enforced by static_assert), so the instantiation is rejected at compile time. See lean/README.md deviation 1.",
  "strings/basic.string/string.nonmembers/string.io/get_line.pass.cpp": "lean deviation 1 (custom allocators): this test instantiates std::basic_string with a non-std allocator (test_allocator/min_allocator/safe_allocator/limited_alloc) or checks allocator-dependent noexcept/propagation. lean supports only std::allocator<CharT> (enforced by static_assert), so the instantiation is rejected at compile time. See lean/README.md deviation 1.",
  "strings/basic.string/string.nonmembers/string.io/get_line_delim.pass.cpp": "lean deviation 1 (custom allocators): this test instantiates std::basic_string with a non-std allocator (test_allocator/min_allocator/safe_allocator/limited_alloc) or checks allocator-dependent noexcept/propagation. lean supports only std::allocator<CharT> (enforced by static_assert), so the instantiation is rejected at compile time. See lean/README.md deviation 1.",
  "strings/basic.string/string.nonmembers/string.io/get_line_delim_rv.pass.cpp": "lean deviation 1 (custom allocators): this test instantiates std::basic_string with a non-std allocator (test_allocator/min_allocator/safe_allocator/limited_alloc) or checks allocator-dependent noexcept/propagation. lean supports only std::allocator<CharT> (enforced by static_assert), so the instantiation is rejected at compile time. See lean/README.md deviation 1.",
  "strings/basic.string/string.nonmembers/string.io/get_line_rv.pass.cpp": "lean deviation 1 (custom allocators): this test instantiates std::basic_string with a non-std allocator (test_allocator/min_allocator/safe_allocator/limited_alloc) or checks allocator-dependent noexcept/propagation. lean supports only std::allocator<CharT> (enforced by static_assert), so the instantiation is rejected at compile time. See lean/README.md deviation 1.",
  "strings/basic.string/string.nonmembers/string.io/stream_extract.pass.cpp": "lean deviation 1 (custom allocators): this test instantiates std::basic_string with a non-std allocator (test_allocator/min_allocator/safe_allocator/limited_alloc) or checks allocator-dependent noexcept/propagation. lean supports only std::allocator<CharT> (enforced by static_assert), so the instantiation is rejected at compile time. See lean/README.md deviation 1.",
  "strings/basic.string/string.nonmembers/string.io/stream_insert.pass.cpp": "lean deviation 1 (custom allocators): this test instantiates std::basic_string with a non-std allocator (test_allocator/min_allocator/safe_allocator/limited_alloc) or checks allocator-dependent noexcept/propagation. lean supports only std::allocator<CharT> (enforced by static_assert), so the instantiation is rejected at compile time. See lean/README.md deviation 1.",
  "strings/basic.string/string.nonmembers/string.special/swap_noexcept.pass.cpp": "lean deviation 1 (custom allocators): this test instantiates std::basic_string with a non-std allocator (test_allocator/min_allocator/safe_allocator/limited_alloc) or checks allocator-dependent noexcept/propagation. lean supports only std::allocator<CharT> (enforced by static_assert), so the instantiation is rejected at compile time. See lean/README.md deviation 1.",
  "strings/basic.string/string.nonmembers/string_op+/allocator_propagation.pass.cpp": "lean deviation 1 (custom allocators): this test instantiates std::basic_string with a non-std allocator (test_allocator/min_allocator/safe_allocator/limited_alloc) or checks allocator-dependent noexcept/propagation. lean supports only std::allocator<CharT> (enforced by static_assert), so the instantiation is rejected at compile time. See lean/README.md deviation 1.",
  "strings/basic.string/string.ops/string_substr/substr.pass.cpp": "lean deviation 1 (custom allocators): this test instantiates std::basic_string with a non-std allocator (test_allocator/min_allocator/safe_allocator/limited_alloc) or checks allocator-dependent noexcept/propagation. lean supports only std::allocator<CharT> (enforced by static_assert), so the instantiation is rejected at compile time. See lean/README.md deviation 1.",
  "strings/basic.string/types.pass.cpp": "lean deviation 1 (custom allocators): this test instantiates std::basic_string with a non-std allocator (test_allocator/min_allocator/safe_allocator/limited_alloc) or checks allocator-dependent noexcept/propagation. lean supports only std::allocator<CharT> (enforced by static_assert), so the instantiation is rejected at compile time. See lean/README.md deviation 1.",
  "strings/strings.erasure/erase.pass.cpp": "lean deviation 1 (custom allocators): this test instantiates std::basic_string with a non-std allocator (test_allocator/min_allocator/safe_allocator/limited_alloc) or checks allocator-dependent noexcept/propagation. lean supports only std::allocator<CharT> (enforced by static_assert), so the instantiation is rejected at compile time. See lean/README.md deviation 1.",
  "strings/strings.erasure/erase_if.pass.cpp": "lean deviation 1 (custom allocators): this test instantiates std::basic_string with a non-std allocator (test_allocator/min_allocator/safe_allocator/limited_alloc) or checks allocator-dependent noexcept/propagation. lean supports only std::allocator<CharT> (enforced by static_assert), so the instantiation is rejected at compile time. See lean/README.md deviation 1.",
  "strings/basic.string/string.access/at.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.access/back.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.access/front.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.access/index.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.capacity/clear.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.capacity/empty.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.capacity/length.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.capacity/reserve_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.capacity/resize_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.capacity/resize_size_char.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.capacity/shrink_to_fit.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.capacity/size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.cons/T_size_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.cons/char_assignment.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.cons/copy.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.cons/copy_alloc.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.cons/copy_assignment.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.cons/initializer_list.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.cons/initializer_list_assignment.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.cons/move.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.cons/move_assignment.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.cons/pointer_alloc.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.cons/pointer_assignment.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.cons/pointer_size_alloc.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.cons/size_char_alloc.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.cons/string_view.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.cons/string_view_assignment.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.cons/substr.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.iterators/begin.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.iterators/cbegin.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.iterators/cend.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.iterators/crbegin.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.iterators/crend.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.iterators/end.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.iterators/rbegin.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.iterators/rend.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_append/T_size_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_append/initializer_list.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_append/iterator.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_append/pointer.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_append/pointer_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_append/size_char.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_append/string.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_append/string_size_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_append/string_view.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_assign/T_size_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_assign/initializer_list.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_assign/iterator.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_assign/pointer.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_assign/pointer_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_assign/rv_string.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_assign/size_char.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_assign/string_size_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_assign/string_view.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_copy/copy.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_erase/iter.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_erase/iter_iter.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_erase/pop_back.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_erase/size_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_insert/iter_char.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_insert/iter_initializer_list.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_insert/iter_iter_iter.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_insert/iter_size_char.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_insert/size_T_size_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_insert/size_pointer.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_insert/size_pointer_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_insert/size_size_char.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_insert/size_string.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_insert/size_string_size_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_insert/string_view.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_op_plus_equal/char.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_op_plus_equal/initializer_list.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_op_plus_equal/pointer.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_op_plus_equal/string.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_replace/iter_iter_initializer_list.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_replace/iter_iter_iter_iter.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_replace/iter_iter_pointer.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_replace/iter_iter_pointer_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_replace/iter_iter_size_char.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_replace/iter_iter_string.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_replace/iter_iter_string_view.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_replace/size_size_T_size_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_replace/size_size_pointer.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_replace/size_size_pointer_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_replace/size_size_size_char.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_replace/size_size_string.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_replace/size_size_string_size_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_replace/size_size_string_view.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.modifiers/string_swap/swap.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string.special/swap.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string_op!=/pointer_string.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string_op!=/string_pointer.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string_op!=/string_string.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string_op!=/string_string_view.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string_op!=/string_view_string.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string_op+/char_string.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string_op+/pointer_string.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string_op+/string_char.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string_op+/string_pointer.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string_op+/string_string.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string_operator==/pointer_string.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string_operator==/string_pointer.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string_operator==/string_string.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string_operator==/string_string_view.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string_operator==/string_view_string.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string_opgt/pointer_string.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string_opgt/string_pointer.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string_opgt/string_string.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string_opgt/string_string_view.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string_opgt/string_view_string.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string_opgt=/pointer_string.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string_opgt=/string_pointer.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string_opgt=/string_string.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string_opgt=/string_string_view.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string_opgt=/string_view_string.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string_oplt/pointer_string.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string_oplt/string_pointer.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string_oplt/string_string.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string_oplt/string_string_view.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string_oplt/string_view_string.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string_oplt=/pointer_string.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string_oplt=/string_pointer.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string_oplt=/string_string.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string_oplt=/string_string_view.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.nonmembers/string_oplt=/string_view_string.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string.accessors/c_str.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string.accessors/data.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string.accessors/get_allocator.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_compare/pointer.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_compare/size_size_T_size_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_compare/size_size_pointer.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_compare/size_size_pointer_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_compare/size_size_string.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_compare/size_size_string_size_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_compare/size_size_string_view.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_compare/string.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_compare/string_view.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_find.first.not.of/char_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_find.first.not.of/pointer_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_find.first.not.of/pointer_size_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_find.first.not.of/string_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_find.first.not.of/string_view_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_find.first.of/char_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_find.first.of/pointer_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_find.first.of/pointer_size_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_find.first.of/string_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_find.first.of/string_view_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_find.last.not.of/char_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_find.last.not.of/pointer_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_find.last.not.of/pointer_size_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_find.last.not.of/string_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_find.last.not.of/string_view_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_find.last.of/char_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_find.last.of/pointer_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_find.last.of/pointer_size_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_find.last.of/string_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_find.last.of/string_view_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_find/char_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_find/pointer_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_find/pointer_size_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_find/string_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_find/string_view_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_rfind/char_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_rfind/pointer_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_rfind/pointer_size_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_rfind/string_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.ops/string_rfind/string_view_size.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.require/contiguous.pass.cpp": "lean deviations 1+2+3: static_assert(test()) constant-evaluates test(), which constructs NON-EMPTY std::string at compile time (lean has no constexpr heap and no SSO, so only the EMPTY string is constexpr-constructible), and the same test() additionally instantiates basic_string with a custom allocator (min_allocator/test_allocator) which lean rejects. Either deviation alone makes the test inapplicable; the std::string runtime coverage and the constexpr-empty slice remain exercised by lean/test/string.cpp. See lean/README.md deviations 1, 2, 3.",
  "strings/basic.string/string.cons/brace_assignment.pass.cpp": "lean deviations 2+3 (no constexpr heap, no SSO): the runtime test PASSES under lean (verified), but static_assert(test()) additionally forces CONSTANT EVALUATION of test(), which constructs a non-empty std::string. lean always heap-allocates (or points at the shared static empty rep), so only the EMPTY string is constexpr-constructible; base passes solely because its SSO puts small strings in-object. The lost coverage is the compile-time evaluation of non-empty string ops; the runtime path is retained. See lean/README.md deviations 2 & 3.",
  "strings/basic.string/string.ends_with/ends_with.char.pass.cpp": "lean deviations 2+3 (no constexpr heap, no SSO): the runtime test PASSES under lean (verified), but static_assert(test()) additionally forces CONSTANT EVALUATION of test(), which constructs a non-empty std::string. lean always heap-allocates (or points at the shared static empty rep), so only the EMPTY string is constexpr-constructible; base passes solely because its SSO puts small strings in-object. The lost coverage is the compile-time evaluation of non-empty string ops; the runtime path is retained. See lean/README.md deviations 2 & 3.",
  "strings/basic.string/string.ends_with/ends_with.ptr.pass.cpp": "lean deviations 2+3 (no constexpr heap, no SSO): the runtime test PASSES under lean (verified), but static_assert(test()) additionally forces CONSTANT EVALUATION of test(), which constructs a non-empty std::string. lean always heap-allocates (or points at the shared static empty rep), so only the EMPTY string is constexpr-constructible; base passes solely because its SSO puts small strings in-object. The lost coverage is the compile-time evaluation of non-empty string ops; the runtime path is retained. See lean/README.md deviations 2 & 3.",
  "strings/basic.string/string.ends_with/ends_with.string_view.pass.cpp": "lean deviations 2+3 (no constexpr heap, no SSO): the runtime test PASSES under lean (verified), but static_assert(test()) additionally forces CONSTANT EVALUATION of test(), which constructs a non-empty std::string. lean always heap-allocates (or points at the shared static empty rep), so only the EMPTY string is constexpr-constructible; base passes solely because its SSO puts small strings in-object. The lost coverage is the compile-time evaluation of non-empty string ops; the runtime path is retained. See lean/README.md deviations 2 & 3.",
  "strings/basic.string/string.iterators/iterators.pass.cpp": "lean deviations 2+3 (no constexpr heap, no SSO): the runtime test PASSES under lean (verified), but static_assert(test()) additionally forces CONSTANT EVALUATION of test(), which constructs a non-empty std::string. lean always heap-allocates (or points at the shared static empty rep), so only the EMPTY string is constexpr-constructible; base passes solely because its SSO puts small strings in-object. The lost coverage is the compile-time evaluation of non-empty string ops; the runtime path is retained. See lean/README.md deviations 2 & 3.",
  "strings/basic.string/string.modifiers/robust_against_adl.pass.cpp": "lean deviations 2+3 (no constexpr heap, no SSO): the runtime test PASSES under lean (verified), but static_assert(test()) additionally forces CONSTANT EVALUATION of test(), which constructs a non-empty std::string. lean always heap-allocates (or points at the shared static empty rep), so only the EMPTY string is constexpr-constructible; base passes solely because its SSO puts small strings in-object. The lost coverage is the compile-time evaluation of non-empty string ops; the runtime path is retained. See lean/README.md deviations 2 & 3.",
  "strings/basic.string/string.nonmembers/string.cmp/comparison.pass.cpp": "lean deviations 2+3 (no constexpr heap, no SSO): the runtime test PASSES under lean (verified), but static_assert(test()) additionally forces CONSTANT EVALUATION of test(), which constructs a non-empty std::string. lean always heap-allocates (or points at the shared static empty rep), so only the EMPTY string is constexpr-constructible; base passes solely because its SSO puts small strings in-object. The lost coverage is the compile-time evaluation of non-empty string ops; the runtime path is retained. See lean/README.md deviations 2 & 3.",
  "strings/basic.string/string.starts_with/starts_with.char.pass.cpp": "lean deviations 2+3 (no constexpr heap, no SSO): the runtime test PASSES under lean (verified), but static_assert(test()) additionally forces CONSTANT EVALUATION of test(), which constructs a non-empty std::string. lean always heap-allocates (or points at the shared static empty rep), so only the EMPTY string is constexpr-constructible; base passes solely because its SSO puts small strings in-object. The lost coverage is the compile-time evaluation of non-empty string ops; the runtime path is retained. See lean/README.md deviations 2 & 3.",
  "strings/basic.string/string.starts_with/starts_with.ptr.pass.cpp": "lean deviations 2+3 (no constexpr heap, no SSO): the runtime test PASSES under lean (verified), but static_assert(test()) additionally forces CONSTANT EVALUATION of test(), which constructs a non-empty std::string. lean always heap-allocates (or points at the shared static empty rep), so only the EMPTY string is constexpr-constructible; base passes solely because its SSO puts small strings in-object. The lost coverage is the compile-time evaluation of non-empty string ops; the runtime path is retained. See lean/README.md deviations 2 & 3.",
  "strings/basic.string/string.starts_with/starts_with.string_view.pass.cpp": "lean deviations 2+3 (no constexpr heap, no SSO): the runtime test PASSES under lean (verified), but static_assert(test()) additionally forces CONSTANT EVALUATION of test(), which constructs a non-empty std::string. lean always heap-allocates (or points at the shared static empty rep), so only the EMPTY string is constexpr-constructible; base passes solely because its SSO puts small strings in-object. The lost coverage is the compile-time evaluation of non-empty string ops; the runtime path is retained. See lean/README.md deviations 2 & 3.",
  "strings/string.view/string.view.comparison/comparison.pass.cpp": "lean deviations 2+3 (no constexpr heap, no SSO): the runtime test PASSES under lean (verified), but static_assert(test()) additionally forces CONSTANT EVALUATION of test(), which constructs a non-empty std::string. lean always heap-allocates (or points at the shared static empty rep), so only the EMPTY string is constexpr-constructible; base passes solely because its SSO puts small strings in-object. The lost coverage is the compile-time evaluation of non-empty string ops; the runtime path is retained. See lean/README.md deviations 2 & 3.",
  "strings/string.view/string.view.comparison/equal.pass.cpp": "lean deviations 2+3 (no constexpr heap, no SSO): the runtime test PASSES under lean (verified), but static_assert(test()) additionally forces CONSTANT EVALUATION of test(), which constructs a non-empty std::string. lean always heap-allocates (or points at the shared static empty rep), so only the EMPTY string is constexpr-constructible; base passes solely because its SSO puts small strings in-object. The lost coverage is the compile-time evaluation of non-empty string ops; the runtime path is retained. See lean/README.md deviations 2 & 3.",
  "strings/string.view/string.view.comparison/greater.pass.cpp": "lean deviations 2+3 (no constexpr heap, no SSO): the runtime test PASSES under lean (verified), but static_assert(test()) additionally forces CONSTANT EVALUATION of test(), which constructs a non-empty std::string. lean always heap-allocates (or points at the shared static empty rep), so only the EMPTY string is constexpr-constructible; base passes solely because its SSO puts small strings in-object. The lost coverage is the compile-time evaluation of non-empty string ops; the runtime path is retained. See lean/README.md deviations 2 & 3.",
  "strings/string.view/string.view.comparison/greater_equal.pass.cpp": "lean deviations 2+3 (no constexpr heap, no SSO): the runtime test PASSES under lean (verified), but static_assert(test()) additionally forces CONSTANT EVALUATION of test(), which constructs a non-empty std::string. lean always heap-allocates (or points at the shared static empty rep), so only the EMPTY string is constexpr-constructible; base passes solely because its SSO puts small strings in-object. The lost coverage is the compile-time evaluation of non-empty string ops; the runtime path is retained. See lean/README.md deviations 2 & 3.",
  "strings/string.view/string.view.comparison/less.pass.cpp": "lean deviations 2+3 (no constexpr heap, no SSO): the runtime test PASSES under lean (verified), but static_assert(test()) additionally forces CONSTANT EVALUATION of test(), which constructs a non-empty std::string. lean always heap-allocates (or points at the shared static empty rep), so only the EMPTY string is constexpr-constructible; base passes solely because its SSO puts small strings in-object. The lost coverage is the compile-time evaluation of non-empty string ops; the runtime path is retained. See lean/README.md deviations 2 & 3.",
  "strings/string.view/string.view.comparison/less_equal.pass.cpp": "lean deviations 2+3 (no constexpr heap, no SSO): the runtime test PASSES under lean (verified), but static_assert(test()) additionally forces CONSTANT EVALUATION of test(), which constructs a non-empty std::string. lean always heap-allocates (or points at the shared static empty rep), so only the EMPTY string is constexpr-constructible; base passes solely because its SSO puts small strings in-object. The lost coverage is the compile-time evaluation of non-empty string ops; the runtime path is retained. See lean/README.md deviations 2 & 3.",
  "strings/string.view/string.view.comparison/not_equal.pass.cpp": "lean deviations 2+3 (no constexpr heap, no SSO): the runtime test PASSES under lean (verified), but static_assert(test()) additionally forces CONSTANT EVALUATION of test(), which constructs a non-empty std::string. lean always heap-allocates (or points at the shared static empty rep), so only the EMPTY string is constexpr-constructible; base passes solely because its SSO puts small strings in-object. The lost coverage is the compile-time evaluation of non-empty string ops; the runtime path is retained. See lean/README.md deviations 2 & 3."
}