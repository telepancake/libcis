// transferred+adapted from libc++ by tools/transfer.py (slug=depr_depr_c_headers_stddef_h_nullptr_3a06ab87).
//===----------------------------------------------------------------------===//
//
// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
// See https://llvm.org/LICENSE.txt for license information.
// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
//
//===----------------------------------------------------------------------===//

// <stddef.h>

#include <stddef.h>
#include <cassert>

namespace libcis_ns_depr_depr_c_headers_stddef_h_nullptr_3a06ab87 { // libcis
int main(int, char**) {
    {
        void *p = NULL;
        assert(!p);
    }
    {
        void *p = nullptr;
        assert(!p);
    }

    return 0;

    return 0;
}
} // libcis_ns_depr_depr_c_headers_stddef_h_nullptr_3a06ab87 (libcis)

