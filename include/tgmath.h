// <tgmath.h> — type-generic math.
// Ported from libc++ include/tgmath.h: in C++ this header is just the union of
// <cmath> and <complex> (the C type-generic macros are unnecessary because the
// C++ overloads already dispatch on argument type).
#pragma once

#include <cmath>
#include <complex>
