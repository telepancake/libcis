build/
toolchain/
a.out
*.out
__pycache__/
*.pyc
.claude/
# test/std/ is now COMMITTED (pregenerated by the transfer) so the default
# `make test` builds the tests directly without bootstrapping the transfer
# toolchain.  Regenerate + recommit it with `make transfer` only when the libc++
# corpus or the transfer tool (tools/transfer*.py, tools/gen_transfer.py) changes.
libcxx.??????
libstdcxx.??????
*.dat
.ninja_deps
.ninja_log
bench/*.bin

# local harness output
.test_results/
